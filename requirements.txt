# STT
groq

# LLM
google-generativeai

# TTS (local, no API key)
kokoro-onnx

# Audio I/O
sounddevice
numpy

# Env
python-dotenv

# Web search
duckduckgo-search

# ── Phase 2: RAG ──────────────────────────────────────────────────────────────
# Vector database (embedded, local, no server)
lancedb
pyarrow

# Embeddings (local, offline — no API key)
sentence-transformers

# Document ingestion
pypdf              # PDF
python-docx         # DOCX
beautifulsoup4      # HTML
pyyaml              # YAML
